# Supabase Configuration
# Get these values from your Supabase Dashboard:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Navigate to Settings → API
# 4. Copy the Project URL and Anon Key

NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Public site URL (no trailing slash). Used for SEO canonical/OG/sitemap.
# e.g. https://yourblog.com
NEXT_PUBLIC_SITE_URL=https://www.xsooi.com/blog

# Shared secret for POST /api/revalidate. Generate with: openssl rand -hex 32
# Must match the value sent by blog-admin and any Supabase webhook.
REVALIDATE_SECRET=your-revalidate-secret
